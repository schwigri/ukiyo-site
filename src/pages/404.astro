---
import Layout from "@/layouts/Layout.astro";
import { getTranslations } from "@/i18n";

const { s, t } = getTranslations(Astro.currentLocale);
---

<Layout title={s`Page not found`}>
  <h1>{t`Page not found`}</h1>
</Layout>
